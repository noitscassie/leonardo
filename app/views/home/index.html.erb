<%= link_to 'New Post', new_post_url %>

<h2>Posts</h2>
<% @posts.each do |post| %>
  <img src=<%= post.image %>>
  <h3><%= post.description %></h3>
  <p>Created at <%= post.created_at %></p>
<% end %>
